name: Vue Biome Lint

on:
  push:
    branches: [main]
    paths:
      - "packages/vue-treeselectjs/**"
      - ".github/workflows/vue-biome.yml"
  pull_request:
    branches: [main]
    paths:
      - "packages/vue-treeselectjs/**"
      - ".github/workflows/vue-biome.yml"

jobs:
  biome-lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.28.0

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run lint (vue package)
        run: pnpm -C packages/vue-treeselectjs run lint