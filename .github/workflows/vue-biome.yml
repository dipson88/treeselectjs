name: Vue Biome Lint

on:
  push:
    branches: [main]
    paths:
      - "packages/vue-treeselectjs/**"
      - ".github/workflows/vue-biome.yml"
  pull_request:
    branches: [main]
    paths:
      - "packages/vue-treeselectjs/**"
      - ".github/workflows/vue-biome.yml"


env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
jobs:
  biome-lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10.33.0

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run lint (vue package)
        run: pnpm --filter vue-treeselectjs run lint
