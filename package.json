{
  "name": "treeselectjs-monorepo",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "check": "pnpm -r check"
  },
  "pnpm": {
    "overrides": {
      "minimatch": ">=10.2.4"
    }
  }
}
