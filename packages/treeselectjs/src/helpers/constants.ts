/** Default text when no options match search */
export const DEFAULT_EMPTY_TEXT = 'No results found...'

/** Default placeholder for search input */
export const DEFAULT_PLACEHOLDER = 'Search...'

/** Default text for tags count (e.g. "5 elements selected") */
export const DEFAULT_TAGS_COUNT_TEXT = 'elements selected'

/** Debounce delay (ms) for search input */
export const SEARCH_DEBOUNCE_MS = 350

/** Default padding (px) for list items */
export const DEFAULT_ITEM_PADDING = 20

/** Padding (px) for zero-level items when no groups */
export const ZERO_LEVEL_ITEM_PADDING = 5
